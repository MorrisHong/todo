const toDoContainer = document.querySelector(".todo-container");
const doingContainer = document.querySelector(".doing-container");
const doneContainer = document.querySelector(".done-container");
console.log(toDoContainer, doingContainer, doneContainer);

const toDoContentArr = [];
const doingContentArr = [];
const doneContentArr = [];

const toDoObj = {
    content : content,
    name : name,
    date : date,
    order : order,
    button : button
}

const doingObj = {
    content : content,
    name : name,
    date : date,
    order : order,
    button : button
}

const doneObj = {
    content : content,
    name : name,
    date : date,
    order : order
}


function init(){

}
init();